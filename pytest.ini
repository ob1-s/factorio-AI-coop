[pytest]
testpaths = tests
addopts = -ra
markers =
    daemon: wire-level tests that launch the production UDP daemon
    runtime: tests that require a real Factorio process
    static_contract: read-only source contract checks
